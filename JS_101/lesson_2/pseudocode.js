// a function that returns the sum of two numbers 
// Get the first number from the user
// Get the second number from the user
// Set a result variable equal to the first number plus the second number
// Print the result variable to the console.

// a function that takes an array of strings, and returns a 
// string that is all those strings concatenated
// - set a new empty string
// - while not at the last array element 
//   iterate through the given array adding each string element 
//   to the new string.
// once the last element is added Print the new string.

// a function that takes an array of integers, and
// returns a new array of every other element
// - set a new empty array
// - set up a counter at zero 
// - while not at the last element of the array 
//   iterate throught the array starting at the index of the counter
//   add the element to the new array
// - skip the next element and move to the element at index 2
//   by incrementing the counter by 2
// - return the new array